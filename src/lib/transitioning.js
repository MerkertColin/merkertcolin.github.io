const duration = 200;
const delay = duration;

export const outTransitionParams = {
    duration: duration,
    x: '-100vw'
}

export const inTransitionParams = {
    duration: duration,
    delay: delay,
    x: '100vw'
}